{
  "name": "@studio/fail",
  "version": "1.1.0",
  "description": "Fail with an Error object and a conventional code property",
  "main": "lib/fail.js",
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "mocha",
    "posttest": "npm run lint",
    "watch": "mocha --watch",
    "preversion": "npm test",
    "version": "changes --commits",
    "postversion": "git push --follow-tags && npm publish"
  },
  "author": "Maximilian Antoni <max@javascript.studio>",
  "homepage": "https://github.com/javascript-studio/studio-fail",
  "eslintConfig": {
    "extends": "@studio"
  },
  "devDependencies": {
    "@studio/changes": "^1.5.2",
    "@studio/eslint-config": "^1.0.2",
    "eslint": "^3.19.0",
    "mocha": "^3.5.3",
    "sinon": "^2.4.1"
  }
}
